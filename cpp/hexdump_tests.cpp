#include <stdio.h>

#include "hexdump.h"
#include "hexdump_tests.h"


bool hexdump_tests_main(bool verbose)
{
    bool rez = false;
    printf("hexdump_tests_main() = %s\n", (rez) ? "[Passed]" : "[FAILED]"); 
    return false;
}
