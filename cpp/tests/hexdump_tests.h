#include "hexdump.h"

#ifndef __HEXDUMP__TESTS__H__
#define __HEXDUMP__TESTS__H__

// Main testing function, calls the other tests in the suite
// @verbose if true, activates the prints of the inner tests
bool hexdump_tests_main(bool verbose);

#endif // __HEXDUMP__TESTS__H__

